<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-only override of the production policy in src/main.

  Everything still defaults to HTTPS; the exceptions exist so a debug build can be pointed
  at a local FastAPI server with -PAPI_BASE_URL=http://10.0.2.2:8000/ (10.0.2.2 is how the
  Android emulator reaches the host machine). Release builds use the src/main file, which
  has no exceptions at all.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
